language: node_js
node_js:
  - "8"
before_script:
  - npm i -g increase-memory-limit
script:
  - make test # run tests
  - make # ensure /src builds
  - cd docs && npm i && increase-memory-limit && make # ensure /docs builds
after_success:
  - make ci_publish
