---
name: Title
menu: Elements
route: /elements/title
---

import { Playground } from "docz";

import {
  ForwardRefAsExoticComponentDoc,
  mapEnumerable,
  OptionBlock
} from "src/__docs__/components";
import { DEFAULTS } from "src/base/helpers/variables";
import { Block } from "src/elements";
import { Title, TITLE_DEFAULTS } from "../title";

# Title and Subtitle

Simple **headings** to add depth to your page.

Use the `subtitle` prop on `<Title>` to make it into a subtitle.

<Playground>
  <Title>Title</Title>
  <Title subtitle>Title</Title>
</Playground>

### Sizes

Use the `size` prop on `<Title>` to adjust its size.

<Playground>
  {() =>
    TITLE_DEFAULTS.sizes.map((size, i) => (
      <OptionBlock props={{ size }} index={i} key={i}>
        <Title size={size}>
          Title {size} {size === 3 ? "(default size)" : null}
        </Title>
      </OptionBlock>
    ))
  }
</Playground>

<Playground>
  {() =>
    TITLE_DEFAULTS.sizes.map((size, i) => (
      <OptionBlock props={{ size }} index={i} key={i}>
        <Title subtitle size={size}>
          Subtitle {size} {size === 5 ? "(default size)" : null}
        </Title>
      </OptionBlock>
    ))
  }
</Playground>

### Combining

When you combine a title and a subtitle, they move closer together.

As a rule of thumb, use a size difference of **two**. For example, if your title is `size={1}` then make your subtitle `size={3}`.

<Playground>
  {() =>
    [1, 2, 3].map((tsize, i) => (
      <OptionBlock
        props={{ "title.size": tsize, "subtitle.size": tsize + 2 }}
        index={i}
        key={i}
      >
        <Title size={tsize}>Title {tsize}</Title>
        <Title size={tsize + 2} subtitle>
          Subtitle {tsize + 2}
        </Title>
      </OptionBlock>
    ))
  }
</Playground>

Use the `spaced` prop on `<Title>` to maintain spacing between titles and subtitles.

<Playground>
  {() =>
    [1, 2, 3].map((tsize, i) => (
      <OptionBlock
        props={{ "title.size": tsize, "subtitle.size": tsize + 2 }}
        index={i}
        key={i}
      >
        <Title size={tsize} spaced>
          Title {tsize}
        </Title>
        <Title size={tsize + 2} subtitle>
          Subtitle {tsize + 2}
        </Title>
      </OptionBlock>
    ))
  }
</Playground>

## API

<ForwardRefAsExoticComponentDoc
  component={Title}
  customize
  docPath="/elements/title"
  name="Title"
  props={{
    size: {
      description: "set the title's size",
      typeName: "number",
      typeTip: mapEnumerable(TITLE_DEFAULTS.sizes)
    },
    spaced: {
      description: "add space below the title",
      typeName: "boolean"
    },
    subtitle: {
      description: "make it a subtitle",
      typeName: "boolean"
    }
  }}
/>
