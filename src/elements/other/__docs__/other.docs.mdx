---
name: Other
menu: Elements
route: /elements/other
---

import { Playground } from "docz";

import { ForwardRefAsExoticComponentDoc, mapEnumerable } from "src/__docs__";
import { Table } from "src/elements";
import { Block } from "../block";
import { Delete, DELETE_DEFAULTS } from "../delete";
import { Heading } from "../heading";
import { Highlight } from "../highlight";
import { Loader } from "../loader";
import { Numeric } from "../numeric";

# Other Components

These are utility components for the Bulma library.
Often, these components aren't very useful on their own.

### Block

The `<Block>` component is useful for creating vertical space between unrelated content.

<Playground>
  <Block>Block 1</Block>
  <Block>Block 2</Block>
</Playground>

### Delete

A versatile **delete** cross.

Frequently used with the `<Modal>` and `<Notification>` components.

<Playground>
  <Delete />
</Playground>

Use the `size` prop of `<Delete>` to change its size.

<Playground>
  {() => {
    const sizes = DELETE_DEFAULTS.sizes.map(size => ({ name: size, size }));
    sizes.splice(1, 0, { name: "-", size: undefined });

    return (
      <Table bordered>
        <thead>
          <tr>
            <th><code>Delete</code> size</th>
            <th>Result</th>
          </tr>
        </thead>
        <tbody>
          {
            sizes.map(({size, name}) => (
              <tr key={name}>
                <td><code>{name}</code></td>
                <td><Delete size={size} /></td>
              </tr>
            ))
          }
        </tbody>
      </Table>
    );

}}

</Playground>

### Heading

<Playground>
  <Heading>A heading component</Heading>
</Playground>

### Highlight

The `<Highlight>` component extends the styling of the `<Block>` component, but hides overflow and has no padding.

<Playground>
  <Highlight>Highlight 1</Highlight>
  <Highlight>Highlight 2</Highlight>
</Playground>

### Loader

Useful when a user is waiting for content to load.

<Playground>
  <Loader />
</Playground>

### Numeric

<Playground>
  <Numeric>42</Numeric>
</Playground>

## API

<ForwardRefAsExoticComponentDoc component={Block} name="Block" />

<ForwardRefAsExoticComponentDoc
  component={Delete}
  docPath="/elements/delete"
  name="Delete"
  props={{
    size: {
      description: "set the delete's size",
      typeName: "string",
      typeTip: mapEnumerable(DELETE_DEFAULTS.sizes)
    }
  }}
/>

<ForwardRefAsExoticComponentDoc component={Heading} name="Heading" />

<ForwardRefAsExoticComponentDoc component={Highlight} name="Highlight" />

<ForwardRefAsExoticComponentDoc component={Loader} name="Loader" />

<ForwardRefAsExoticComponentDoc component={Numeric} name="Numeric" />
