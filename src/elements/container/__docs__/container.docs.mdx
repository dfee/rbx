---
name: Container
menu: Elements
route: /elements/container
---

import { Playground } from "docz";

import {
  Frame,
  ForwardRefAsExoticComponentDoc,
  mapEnumerable
} from "src/__docs__";
import { DEFAULTS } from "src/base/helpers/variables";
import { Notification } from "src/elements";
import { Container } from "../container";

# Container

A simple container to center your content horizontally.

<Playground>
  <Frame updateHeightDelay={0}>
    <Container>
      <Notification>
        This container is <strong>centered</strong> on desktop.
      </Notification>
    </Container>
  </Frame>
</Playground>

### Fluid

Use the `fluid` prop for the container to fit horizontally with padding.

<Playground>
  <Container fluid>
    <Notification>
      This container is <strong>fluid</strong>: it will have a 32px gap on
      either side, on any viewport size.
    </Notification>
  </Container>
</Playground>

### Breakpoint

<Playground>
  <Frame>
    {DEFAULTS.breakpoints.map(breakpoint => (
      <Container breakpoint={breakpoint} key={breakpoint}>
        <Notification>
          This container is <strong>fullwidth</strong> <em>until</em> the
          <code>{breakpoint}</code> breakpoint.
        </Notification>
      </Container>
    ))}
  </Frame>
</Playground>

## API

<ForwardRefAsExoticComponentDoc
  component={Container}
  docPath="/layout/container"
  name="Container"
  overrides
  props={{
    breakpoint: {
      description:
        "the responsive size at which this container is no longer fullwidth",
      typeName: "string",
      typeTip: mapEnumerable(DEFAULTS.breakpoints)
    },
    fluid: {
      description:
        "whether the container expands to fit horizontally with padding",
      typeName: "boolean"
    }
  }}
/>
