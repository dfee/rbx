---
name: Form / Checkbox
menu: Elements
route: /elements/form-checkbox
---

import { Playground } from "docz";

import {
  ForwardRefAsExoticComponentDoc,
  mapEnumerable
} from "src/__docs__/components";
import { DEFAULTS } from "src/base/helpers/variables";
import { Checkbox } from "../checkbox";
import { Label } from "../label";

# Checkbox

The 2-state checkbox in its native format.

The `<Checkbox>` component is a simple wrapper around the `<input type="checkbox">` HTML element.
It is intentionally not styled, to preserve cross-browser compatibility and the user experience.

<Playground>
  <Label>
    <Checkbox /> Remember me
  </Label>
</Playground>

You can add **links** to it.

<Playground>
  <Label>
    <Checkbox /> I agree to the <a href="#">terms and conditions</a>
  </Label>
</Playground>

### Disable

Use the standard `disable` prop to disable the checkbox.

<Playground>
  <Label disabled>
    <Checkbox disabled /> Save my preferences
  </Label>
</Playground>

## API

<ForwardRefAsExoticComponentDoc component={Checkbox} docPath="/form/checkbox" />
